# This file contains sensitive information and should not be committed to version control.

# The URL of the backend API
REACT_APP_API_URL=https://api.excel.example.com

# The domain for authentication (e.g., Azure AD domain)
REACT_APP_AUTH_DOMAIN=login.microsoftonline.com

# The client ID for authentication
REACT_APP_AUTH_CLIENT_ID=your-client-id-here

# The URI to redirect to after authentication
REACT_APP_AUTH_REDIRECT_URI=http://localhost:3000/auth-callback

# The name of the Azure Storage account for file storage
REACT_APP_STORAGE_ACCOUNT_NAME=excelstorageaccount

# The instrumentation key for Azure Application Insights
REACT_APP_INSIGHTS_INSTRUMENTATION_KEY=your-instrumentation-key-here

# The version of the frontend application
REACT_APP_VERSION=1.0.0

# The current environment (e.g., development, staging, production)
REACT_APP_ENVIRONMENT=development

# IMPORTANT: The following tasks need to be completed manually:
# 1. Populate the .env file with actual values for the environment variables (Critical)
# 2. Ensure that .env is added to .gitignore to prevent accidental commits (Critical)
# 3. Create a .env.example file with placeholder values as a template (Required)